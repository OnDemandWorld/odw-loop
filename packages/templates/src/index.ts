/**
 * @loop/templates — public API.
 *
 * Exposes the marketplace data model (industries/categories/interfaces) and
 * the TemplateRegistry that loads and serves the template library.
 */

export * from './interface.js';
export * from './registry.js';
