{
  "name": "@loop/triggers",
  "version": "1.0.0",
  "description": "Trigger dispatcher, cron scheduler, webhook ingress, event listener",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "import": "./dist/index.js",
      "types": "./dist/index.d.ts"
    }
  },
  "scripts": {
    "build": "tsc --build",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@loop/types": "workspace:*",
    "@loop/state": "workspace:*",
    "@loop/engine": "workspace:*",
    "@loop/connectors": "workspace:*",
    "@loop/observability": "workspace:*",
    "node-cron": "^3.0.0"
  },
  "devDependencies": {
    "@types/node-cron": "^3.0.0",
    "typescript": "^5.3.0",
    "vitest": "^1.0.0"
  }
}
